<?xml version="1.0" encoding="utf-8"?>
<TableRow
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/available_enrolment_table_row"
    android:weightSum="3">
    <TextView
        android:id="@+id/new_available_enrolment_row_course_code"
        android:layout_width="50dp"
        android:layout_height="wrap_content"
        android:text="Code"
        android:gravity="center"
        android:fontFamily="@font/font_family"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_weight="1"
        android:padding="8dp" />

    <TextView
        android:id="@+id/new_available_enrolment_row_course_name"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Course Name"
        android:fontFamily="@font/font_family"
        android:gravity="center"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_weight="1"
        android:padding="8dp" />

    <Button
        android:id="@+id/new_available_enrolment_row_enrol_button"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:text="Enrol"
        android:textColor="@color/black"
        android:fontFamily="@font/font_family"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_weight="1"
        android:gravity="center"
        android:backgroundTint="@color/light_green"
        android:padding="8dp" />
</TableRow>